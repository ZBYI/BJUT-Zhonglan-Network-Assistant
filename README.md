# 北工大校园网助手

一个面向北京工业大学校园网的 Windows 批处理登录脚本。

## 功能

- 手动双击运行，不常驻后台
- 已经联网时直接退出，不重复认证
- 自动检测校园网认证服务器
- 自动获取当前正在使用的物理网卡 IPv4 地址
- 自动提交 ePortal 登录请求
- 登录后自动验证 Internet 连接
- 使用 UTF-8 中文控制台提示

## 使用方法

1. 下载 `BJUT-Network-Assistant.bat`。
2. 用记事本或其他文本编辑器打开文件。
3. 修改下面两行：

```bat
set "account=YOUR_ACCOUNT@campus"
set "password=YOUR_PASSWORD"
```

4. 以 UTF-8 编码保存。
5. 连接校园网 Wi-Fi 或有线网络后，双击运行脚本。
6. 认证完成后窗口会自动关闭。

## 注意事项

- 请勿把填写了真实账号和密码的版本提交到公开仓库、网盘、聊天群或发送给他人。
- 脚本当前使用的认证地址为 `http://10.21.221.98:801/eportal/portal/login`。如果学校后续调整认证服务器，需要同步修改脚本中的 `server`。
- 不同校区只要使用同一认证服务器和账号域，脚本无需依赖 Wi-Fi 名称。
- 使用前请确认当前连接的是本人有权使用的北京工业大学校园网络。
- 建议保留一份未填写账号和密码的公开版本，仅在本机私人副本中填写真实凭据。

## 隐私与数据说明

- 本项目不包含作者自建服务器、云端后台、数据库、统计平台、广告 SDK、遥测服务或第三方分析组件。
- 脚本不会把用户的校园网账号、密码、IPv4 地址或其他本地信息上传到作者、GitHub 仓库或其他由本项目控制的远程服务。
- 用户填写的账号和密码仅保存在用户自己的本地 BAT 文件中。本项目不会自动读取浏览器密码、Windows 凭据库、文件目录、文档、照片或其他与校园网登录无关的个人数据。
- 进行校园网认证时，账号、密码和当前 IPv4 地址会按照校园网现有 ePortal 协议发送到配置的校园网认证服务器；这是完成登录所必需的网络请求，不属于向本项目作者上传数据。
- 脚本会访问公开网页资源以判断 Internet 是否已经恢复，但该连通性检测请求不会主动附带校园网账号或密码。
- 脚本不会在本地创建用户行为数据库、历史登录记录或遥测日志，也不会在认证完成后常驻后台。当前版本运行结束后即退出。
- 本仓库公开源码中不包含任何用户的真实账号、密码或其他个人凭据。用户应避免将填写真实信息后的本地副本重新提交到公开仓库。
- 由于校园网认证接口当前使用 HTTP，认证数据在传输过程中的保护程度取决于校园网自身的网络环境和认证机制。本项目无法替代 HTTPS，也无法对校园网基础设施的传输安全作出保证。
- 用户可以在运行前直接查看完整源码，确认脚本实际访问的地址、提交的参数及本地执行行为；本项目不存在隐藏的二进制程序或闭源后台组件。

## 免责声明

本项目为个人学习、技术交流及简化本人校园网登录流程而编写的非官方工具，与北京工业大学、学校网络管理部门、校园网运营商、认证系统开发商及其他相关单位均无隶属、合作、授权、背书或官方支持关系。项目名称中出现“北工大”“BJUT”等字样，仅用于说明适用场景和网络环境，不代表任何官方身份。

本项目不提供、鼓励或支持绕过校园网计费、账号权限、设备数量限制、访问控制、安全策略、实名认证要求或其他管理措施。使用者应仅使用本人合法持有并获授权使用的校园网账号，并自行遵守学校现行网络管理规定、账号使用规则以及所在地适用的法律法规。因违规使用账号、共享账号、自动化频率过高或其他不当操作导致的账号限制、网络封禁或其他后果，由使用者自行承担。

本项目依据当前实际测试到的 ePortal 登录流程编写。校园网认证服务器地址、请求参数、账号域、返回格式、网络拓扑及不同校区配置均可能随时调整，因此不保证脚本在未来版本、所有校区、所有网络环境或所有设备上持续可用，也不保证能够兼容学校网络系统后续的任何变更。

脚本需要在本地配置校园网账号和密码。当前 BAT 版本会以明文形式保存在用户自己的脚本文件中，因此请妥善保管该文件，不要将填写真实凭据后的版本上传到 GitHub、公开代码托管平台、网盘或发送给其他人。本仓库公开版本不包含作者或其他用户的真实校园网账号和密码。

当前认证接口使用 HTTP 地址，且认证信息会按照校园网现有 Portal 协议提交至配置的认证服务器。这意味着传输安全性受校园网自身认证机制和当前网络环境约束。对凭据安全有更高要求的用户，应优先使用学校提供的官方认证方式，并自行评估是否适合使用本脚本。

本脚本会访问配置的校园网认证服务器，并访问公开网页资源以检测 Internet 连通状态。除完成校园网认证和网络连通性检测所需的请求外，本项目代码不设计额外的账号收集、遥测、数据上传或后台常驻功能。使用者仍应在运行前自行阅读源码并确认其行为符合自己的安全要求。

本项目按“现状”提供，不对可用性、稳定性、兼容性、准确性、连续性、安全性或特定用途适用性作任何明示或暗示保证。因使用、修改、复制或分发本项目而产生的网络中断、认证失败、账号异常、配置错误、凭据泄露、数据损失或其他直接、间接后果，由使用者自行承担相应风险。

若学校网络管理政策、认证机制或相关规定发生变化，请以学校官方发布的信息为准。本项目中的说明、参数和代码仅代表编写及测试时的实际情况，不应视为北京工业大学校园网的官方文档、技术规范或长期承诺。

## 系统要求

- Windows 10 / Windows 11
- 系统自带 `curl.exe`
- Windows PowerShell

## 工作流程

```text
双击运行
  ↓
检测 Internet
  ├─ 已联网 → 直接退出
  └─ 未联网
       ↓
检测校园网 Portal
       ↓
自动获取 IPv4
       ↓
提交认证
       ↓
验证 Internet
       ↓
自动退出
```
